machine:
  java:
    version: openjdk7
  node:
    version: 6

  environment:
    TERM: dumb
    GRADLE_OPTS: -Xmx1g -Xms1g

dependencies:
  override:
    - npm install
    - npm install -g gulp
    - ./gradlew testClasses appengineDownloadSdk

test:
  override:
    - gulp
    - ./gradlew build
